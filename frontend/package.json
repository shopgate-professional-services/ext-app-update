{
  "name": "@shopgate-project/app-update",
  "version": "1.0.0",
  "description": "Displays a popup or full-page overlay if the app version is lower than a required version.",
  "license": "Apache-2.0",
  "scripts": {
    "test": "jest",
    "test:watch": "npm run test -- --watch",
    "coverage": "jest --coverage",
    "coverage:watch": "npm run coverage -- --watch"
  },
  "devDependencies": {
    "@shopgate/eslint-config": "^7.30.0",
    "@shopgate/engage": "^7.30.0",
    "prop-types": "^15.8.1",
    "react": "~16.6.0",
    "react-dom": "~16.6.0",
    "react-hot-loader": "4.0.1",
    "react-transform-catch-errors": "^1.0.2",
    "redbox-react": "^1.5.0"
  }
}
